import { SearchCoinPipe } from './searchCoin.pipe';

describe('Search.CoinPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCoinPipe();
    expect(pipe).toBeTruthy();
  });
});
