import { FilterMetalPipe } from './filter-metal.pipe';

describe('FilterMetalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMetalPipe();
    expect(pipe).toBeTruthy();
  });
});
