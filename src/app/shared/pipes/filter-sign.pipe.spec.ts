import { FilterSignPipe } from './filter-sign.pipe';

describe('FilterSignPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSignPipe();
    expect(pipe).toBeTruthy();
  });
});
