import { FilterDenominationPipe } from './filter-denomination.pipe';

describe('FilterDenominationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDenominationPipe();
    expect(pipe).toBeTruthy();
  });
});
